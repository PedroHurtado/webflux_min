Solo pasa en un explorador

fetch
xmlhttprequest

Marcadas como inseguras

8080                    8081
Origin:8080             response  Access-Control-Allow-Origin

                        puede devolver http://localhost:8080 | *

*->no se puede autenticar por cookie
Access-Control-Allow-Credentials :true

los dominios permitidos deben de estar en una lista blanca

csrf->get,post,patch,put,delete

EnableCors()->activar el cors por el mismo dominio de entrada

¿Que es el verbo OPTIONS?
Access-Control-Max-Age

1.OPTIONS
2. GET|POST|PUT|DELETE|PATCH